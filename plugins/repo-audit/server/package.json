{
  "name": "repo-audit-server",
  "version": "4.0.0",
  "description": "MCP server for repo-audit — deterministic scan, graph, and findings store",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "build": "node scripts/build.mjs",
    "dist": "npm run vendor && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.7"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@vscode/tree-sitter-wasm": "^0.3.1",
    "esbuild": "^0.28.1",
    "sql.js": "^1.14.1",
    "typescript": "^5.8.3",
    "vitest": "^3.0.0"
  }
}
